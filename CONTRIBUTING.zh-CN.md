# Contributing to Protao

**[English](./CONTRIBUTING.md) · 中文**

Protao 收录的是产品开发全流程中，**当模型能力是现在十倍时依然成立的判断**。

贡献一条经验之前，先问自己这个问题：

> **如果我用一个比现在强得多的模型，这条经验还有意义吗？**
>
> - 有意义 → 它是「道」，可以提交
> - 没意义 → 它是「术」，不属于这里

这不是说术没有价值。是说术会腐烂，道才能沉淀。

---

## 什么可以提交

- **判断**：在某种处境下，哪个选择更好，为什么
- **原则**：一类问题背后共同的解法逻辑
- **反模式**：什么做法看似合理但实际上会出问题，以及为什么

什么不接受：

- 具体工具的使用方法或配置技巧
- 依赖当前模型行为的 prompt 话术
- 以「目前」「现在」「最新版本」开头的建议
- 没有说明「为什么」的操作步骤

---

## 结构：源文件与生成文件

Protao 作为**一个整体 skill** 发布（仓库根目录的 `SKILL.md`），这样 `npx skills add` 会把它当成一个连贯的整体安装。但那个文件是**生成的**——不要手动编辑。

可编辑的源是 `principles/*.md`——每个生命周期领域一个文件（discovery、design、architecture……）。每个都是自洽的一条「道」。修改内容：

1. 编辑对应的 `principles/<领域>.md`
2. 运行 `./build-skill.sh` 重新生成根目录的 `SKILL.md`
3. 两个都提交

这样既保持单一数据源（`principles/` 文件），又能发布成一个合并的 skill。

## 源文件格式

每个 `principles/<领域>.md` 遵循这个格式：

```markdown
---
name: protao-<领域>
description: 一句话说明这个领域覆盖的场景
---

# 标题：一句话概括这个领域

## 核心原则

说清楚：在这个场景下，最重要的判断是什么，为什么。

## 具体行为

列出 agent 应该执行的行为——要问什么、检查什么、什么情况下暂停。

## 红旗清单

- 信号一
- 信号二
```

要求：
- 核心原则：解释「为什么」，不只是「做什么」
- 具体行为：agent 可直接执行的指令，不是泛泛建议
- 红旗清单：agent 能识别的具体信号，不是抽象警告

---

## 如何提交

1. 编辑或新增 `principles/` 下的文件
2. 运行 `./build-skill.sh` 重新生成 `SKILL.md`
3. 确认通过「十倍模型」过滤
4. 提 PR，标题：`principles: <一句话说这条道是什么>`
5. PR 描述里说明这条经验来自什么真实处境

不需要完美。一条有真实依据的粗糙判断，比一条没有来源的精致原则更有价值。
